// Exercise 1: Basic Type Inference
// Understanding when TypeScript can infer types vs when explicit annotations are needed

// TODO: Define a variable `a` with an explicit type annotation of `boolean` and assign it a value of `true`
// Hint: TypeScript needs explicit annotation here for clarity in certain contexts

// TODO: Define a variable `b` without an explicit type annotation and assign it a value of `42`
// Hint: TypeScript can infer this type automatically

// TODO: Think about why TypeScript can infer the type of `b` but an explicit type is provided for `a`
// Consider: What makes one case require explicit typing while the other doesn't?

// TODO: Log both variables to the console to see their values and types

// TODO: Try changing the value of `b` to a string and see what TypeScript tells you
// This will help you understand type inference better

export {};
